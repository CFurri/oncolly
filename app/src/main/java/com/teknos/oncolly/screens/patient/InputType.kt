package com.teknos.oncolly.screens.patient

enum class InputType {
    NUMBER,     // Teclat numèric (ex: minuts, passos)
    TEXT,       // Text curt (ex: nom medicament)
    TEXT_AREA,  // Text llarg (ex: notes)
    BOOLEAN     // Botó Si/No (ex: Has fet els exercicis?)
}